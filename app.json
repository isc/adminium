{
  "environments": {
    "test": {
      "scripts": {
        "test": "eval $(ci env) && rails test"
      },
      "addons":[
         "heroku-redis",
         "heroku-postgresql"
      ]
    }
  }
}
