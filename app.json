{
  "environments": {
    "test": {
      "addons":[
         "heroku-redis",
         "heroku-postgresql",
         "heroku-postgresql"
      ],
      "scripts": {
        "test-setup": "bin/rails assets:precompile"
      }
    }
  }
}
