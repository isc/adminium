- if session[:account] == ENV['DEMO_ACCOUNT_ID']
  .navbar.demo
    .navbar-inner
      .container-fluid
        .span6
          h4 Hello and welcome,
          p
            'This demo account features a sample database.
            'Feel free to play with the data, it will be reset automatically.
        .span3
          - if session[:account_before_demo]
            = link_to stop_demo_docs_path, class: 'btn btn-warning' do
              i.icon-arrow-right.icon-white
              '  Back to my account
          - else
            = link_to 'add adminium', "https://addons.heroku.com/adminium", class: "btn btn-success btn-large"
            p Try it for free on your own Heroku application.
