table.table
  thead
    tr
      th Validator
      th Column
      th
  tbody
    - clazz.settings.validations.each do |validation|
      tr
        td= validation['validator'].humanize
        td= validation['column_name']
        td
          i.icon-remove-sign.remove
.control-group
  label Validator
  .controls
    select
      - %w(validates_presence_of validates_uniqueness_of).each do |validator|
        option value=validator = validator.humanize
input type="hidden" name="validations[]"
.params.hide
  - clazz.settings.validations.each do |validation|
    = hidden_field_tag 'validations[][validator]', validation['validator']
    = hidden_field_tag 'validations[][column_name]', validation['column_name']
.control-group
  label Column
  .controls
    select
      - clazz.settings.columns_options(:form, only_checked: true).each do |name|
        option value=name = name
.control-group
  .controls
    a.btn Add
