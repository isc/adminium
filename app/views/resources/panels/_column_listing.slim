ul id="#{type}_columns_list" data-type=type
  - clazz.settings.columns_options(type).each do |name, checked|
    li.setting_attribute
      - input_id = "#{type}_columns_#{name}".parameterize
      input type='checkbox' name="#{type}_columns[]" id=input_id checked=checked value=name
      label for=input_id = name.humanize
      - if defined? sortable
        i.icon-resize-vertical
