# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  export:
    csv:
      encoding_to_help: Choose your output encoding.
      skip_header_help: Do not output a header (no fields description).
      col_sep: Column separator
      col_sep_help: Choose the character output between columns.
  activerecord:
    attributes:
      account:
        db_url: Database URL
  items:
    destroy_warning: Are you sure that you want to destroy that record?